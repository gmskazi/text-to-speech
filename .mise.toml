[tools]
python = "3.12"

[env]
_.python.venv = { path = ".venv", create = true, uv_create_args = ["--seed"] }

[tasks.install]
description = "Install dependencies"
run = "pip install edge-tts"

[tasks.run]
description = "Generate the Japanese audio"
depends = ["install"]
run = "python roleplay.py"
